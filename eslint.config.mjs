import nextConfig from "eslint-config-next";

// eslint-config-next@16 ships a native flat-config array that includes
// core-web-vitals + typescript presets, so we just re-export it.
export default nextConfig;
